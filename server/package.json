{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run ./dist/index.js",
    "dev": "bun run src/index.ts",
    "watch": "bun --watch src/index.ts",
    "hot": "bun --hot src/index.ts",
    "build": "bun build src/index.ts --out-dir dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}